{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd renderer && npm install && npx vite build"
  },
  "deploy": {
    "startCommand": "node web.js",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
